/* let Element = document.getElementById("data");
let registered_client = document.getElementById("registered-client");

let add_car = document.getElementById("add-car"); */


/* // function for fetch
function fetchData(id) {
    fetch(id)
        .then(response => response.text())
        .then(data => Element.innerHTML = data)
        .catch(error => console.log(error));
}

//registered user
registered_client.addEventListener('click', (e) => {
    e.preventDefault();
    fetchData("Registered_client.php");

});

//add car

add_car.addEventListener('click', (e) => {
    e.preventDefault();
    fetchData("add_car.php");
}); */

// For logout section in dashboard